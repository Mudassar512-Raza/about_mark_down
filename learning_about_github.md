> ## in this lecture we will learn how to use the github.

>## git ??
### git is track change in the code, basically git is a version control system.
 + Git track the history from where we did the our last work.
  - help to collaborate.
  >## GIT HUB 
  * upload all the code,its is websites.
  * [link](https://github.com)
  * read_me file contain the brief explanation ba about the code or project.
>### how to change the code?
- it first the new code in the 
- and the make the command, commmit to save the save the chages.
>## so here we are learning the command of Git in Vs code.
1. clone (clone the repository in  the local machine(computer,laptop,mahcine))
    - syntx  git clone link from the git hub
    - jab ham clone kar chukay ho to file ma janay key ley ham **cd** ,means change directory
2. status(checking the current status )
  